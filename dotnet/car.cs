using System;
namespace myclass{
class Car
{
     int speed;
     string color;

   static void Main(string[] args)
{
     Car car = new Car();
     car.speed = 200;
     car.color = red;
     Cnsole.WriteLine(car.speed);
     Console.WriteLine(car.red);
}
}
}